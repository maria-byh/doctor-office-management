export type Props = any;

export type RegisterProps = {
    changeContentHandler: (component: string) => void;
};
  

export type LoginProps = {
    changeContentHandler: (component: string) => void;
};